<h1>编辑报名资料<</h1>

<%= form_for [:admin, @event, @registration] do |f| %>

  <%= render :partial => "form", :locals => { :f => f } %>

  <div class="form-group">
    <%= f.submit "更新", :class => "btn btn-primary" %>
    <%= link_to "取消", admin_event_registrations_path(@event) %>
  </div>

<% end %>